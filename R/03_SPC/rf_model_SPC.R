# =============================================================================
# rf_model_SPC.R
# Study 2 -- train the percent cover (SPC) Random Forest regression model -- NOTE: you also have rf_model_SPC2.R on disk. Paste whichever is the final version -- see README Open Items.
#
# STATUS: placeholder. Paste your original script's content below, replacing
# this comment block. Keep the filename as-is so the pipeline stays traceable
# to what's described in the paper.
# =============================================================================
